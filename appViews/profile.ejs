<!doctype html>
<html lang="en">

<head>
    <% include partials/head %>
</head>

<body>
    <header>
        <% include partials/header %>
    </header>

    <main>
        <h1>My Bucket List</h1>

            <div class="row">
              <% if (user.parksBucketList.length > 0) { %>
                <% for (let i=0; i < user.parksBucketList.length; i++) { %>
                    <div class="col s12 m4">
                        <div class="card teal darken-3">
                            <div class="card-content white-text">
                                <span class="card-title"> PARK NAME HERE </span>
                                <p>
                                    <%= user.parksBucketList[i] %>
                                </p>
                            </div>
                            <div class="card-action">
                                <a href="parks/<%= user.parksBucketList[i] %>">Learn More</a>
                                <a href="#">Mark Visited</a>
                            </div>
                        </div>
                    </div>
                <% } %>
              <% } %>
            </div>


        <a class="waves-effect waves-light btn-large" href="/parks">All Parks</a>
    </main>

</body>

</html>
